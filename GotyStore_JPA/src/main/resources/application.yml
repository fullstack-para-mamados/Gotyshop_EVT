spring:
  application:
    name: GotyStore
  datasource:
    url: ${GOTYSTORE_DB_URL:jdbc:sqlite:gotystore.db}
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: none
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: ${GOTYSTORE_PORT:8080}

jwt:
  secret: ${JWT_SECRET:GotyStoreSecretKey2024GotyStoreSecretKey2024GotyStoreSecretKey2024}
  expiration: ${JWT_EXPIRATION:86400000}

usuario:
  service:
    url: ${USUARIO_SERVICE_URL:http://localhost:8081}

logging:
  level:
    com.example.GotyStore: DEBUG

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
